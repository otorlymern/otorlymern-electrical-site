# Patch Notation Builder

- Open `index.html` in a static server (VS Code Live Server or `python -m http.server`) to use the builder.
- If icons change, regenerate the palette manifest from `PT_Symbols_SVG/` with:

```
node generate-icons-manifest.mjs
```
